name: 钛屑转运提醒

on:
  workflow_dispatch:
  schedule:
    # 北京时间 9:00 = UTC 1:00
    - cron: '0 1 * * *'
    # 北京时间 11:00 = UTC 3:00
    - cron: '0 3 * * *'
    # 北京时间 13:00 = UTC 5:00
    - cron: '0 5 * * *'
    # 北京时间 15:00 = UTC 7:00
    - cron: '0 7 * * *'

jobs:
  send-message:
    runs-on: ubuntu-latest
    steps:
      - name: 发送企业微信提醒
        run: |
          curl -X POST '你的Webhook地址' \
            -H 'Content-Type: application/json' \
            -d '{
              "msgtype": "text",
              "text": {
                "content": "今日是否有转运钛屑？如已转运请在群内回复"已转运""
              }
            }'
